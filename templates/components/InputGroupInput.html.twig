{% props
    type = 'text',
%}

<input
    type="{{ type }}"
    data-slot="input-group-control"
    class="{{ cn('flex-1 rounded-none border-0 bg-transparent shadow-none focus-visible:ring-0 dark:bg-transparent', attributes.render('class')) }}"
    {{ attributes }}
>
