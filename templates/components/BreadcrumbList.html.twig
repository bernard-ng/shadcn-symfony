<ol
    data-slot="breadcrumb-list"
    class="{{ cn('text-muted-foreground flex flex-wrap items-center gap-1.5 text-sm break-words sm:gap-2.5', attributes.render('class')) }}"
    {{ attributes }}
>
    {% block content %}{% endblock %}
</ol>
