<span
    data-slot="avatar-fallback"
    class="{{ cn('bg-muted flex size-full items-center justify-center rounded-full', attributes.render('class')) }}"
    {{ attributes }}
>
    {% block content %}{% endblock %}
</span>
