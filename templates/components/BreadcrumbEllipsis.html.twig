<span
    data-slot="breadcrumb-ellipsis"
    role="presentation"
    aria-hidden="true"
    class="{{ cn('flex size-9 items-center justify-center', attributes.render('class')) }}"
    {{ attributes }}
>
    <twig:ux:icon name="lucide:more-horizontal" class="size-4" />
    <span class="sr-only">More</span>
</span>
