<div
    data-slot="field-content"
    class="{{ cn('group/field-content flex flex-1 flex-col gap-1.5 leading-snug', attributes.render('class')) }}"
    {{ attributes }}
>
    {% block content %}{% endblock %}
</div>
