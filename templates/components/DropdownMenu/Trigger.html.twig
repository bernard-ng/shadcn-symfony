{% props
    type = 'button',
%}

<button
    type="{{ type }}"
    data-slot="dropdown-menu-trigger"
    class="{{ cn(attributes.render('class')) }}"
    {{ attributes }}
>
    {% block content %}{% endblock %}
</button>
