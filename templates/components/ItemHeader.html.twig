<div
    data-slot="item-header"
    class="{{ cn('flex basis-full items-center justify-between gap-2', attributes.render('class')) }}"
    {{ attributes }}
>
    {% block content %}{% endblock %}
</div>
