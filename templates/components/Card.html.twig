<div
    data-slot="card"
    class="{{ cn("bg-card text-card-foreground flex flex-col gap-6 rounded-xl border py-6 shadow-sm", attributes.render('class')) }}"
    {{ attributes }}
>
    {% block content %}{% endblock %}
</div>
