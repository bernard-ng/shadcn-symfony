<span
    class="{{ cn('text-muted-foreground flex items-center gap-2 text-sm [&_svg]:pointer-events-none [&_svg:not([class*="size-"])]:size-4', attributes.render('class')) }}"
    {{ attributes }}
>
    {% block content %}{% endblock %}
</span>
